[mysqld]
datadir = /var/lib/mysql
server_id = 1
binlog_format = ROW
log_bin = mysql-bin
log_bin_index = /var/lib/mysql/mysql-bin.index
gtid_mode = ON
enforce_gtid_consistency = ON
# Configure replication only after WAL-G binlog-server is listening.
skip_slave_start = ON
